<!-- Striped rows Light Starts-->
<section id="striped-light">
  <div class="row">
    <div class="col-sm-12">
      <div class="card">
        <div class="card-header">
          <div class="card-title-wrap bar-info">
            <h4 class="card-title d-inline">Registered Agents</h4>
          </div>
        </div>

        <div class="card-body">
          <div class="card-block">

            {{#if agents}}

            <table class="table table-striped table-hover">
              <thead>
                <tr>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Delete</th>
                </tr>
              </thead>
              <tbody>

                {{#each agents}}
                <tr>
                  <td>{{firstname}}</td>
                  <td>{{lastname}}</td>
                  <td>{{email}}</td>
                  <td>
                    <form class="d-inline" action="/admin/agents/{{id}}?_method=DELETE" method="post">
                      <button type="submit" class="btn btn-danger btn-round btn-sm shadow-z-2">
                        <i class="icon-trash"></i> Delete
                      </button>
                    </form>
                  </td>
                </tr>
                {{/each}}
              </tbody>
            </table>
            {{else}}
            <p>There is no registered agents yet !</p>
            {{/if}}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<!--Striped rows Light Ends -->